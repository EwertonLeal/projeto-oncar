import { CarBrandListPipe } from './car-brand-list.pipe';

describe('CarBrandListPipe', () => {
  it('create an instance', () => {
    const pipe = new CarBrandListPipe();
    expect(pipe).toBeTruthy();
  });
});
