import { CarYearListPipe } from './car-year-list.pipe';

describe('CarYearListPipe', () => {
  it('create an instance', () => {
    const pipe = new CarYearListPipe();
    expect(pipe).toBeTruthy();
  });
});
