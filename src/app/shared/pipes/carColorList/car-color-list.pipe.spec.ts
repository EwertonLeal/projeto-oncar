import { CarColorListPipe } from './car-color-list.pipe';

describe('CarColorListPipe', () => {
  it('create an instance', () => {
    const pipe = new CarColorListPipe();
    expect(pipe).toBeTruthy();
  });
});
