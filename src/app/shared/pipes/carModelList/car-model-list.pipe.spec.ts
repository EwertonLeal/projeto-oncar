import { CarModelListPipe } from './car-model-list.pipe';

describe('CarModelListPipe', () => {
  it('create an instance', () => {
    const pipe = new CarModelListPipe();
    expect(pipe).toBeTruthy();
  });
});
